<%= form_for([@post, @comment]) do |comment_form| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= comment_form.fields_for :user do |user_form| %>
    <div class="field">
      <%= user_form.label :username %><br />
      <%= user_form.text_field :username, value: (@comment.user.username if @comment.user) %>
    </div>
  <% end %>
  <div class="field">
    <%= comment_form.label :content %><br />
    <%= comment_form.text_area :content %>
  </div>

  <div class="actions">
    <%= comment_form.submit %>
  </div>
<% end %>
